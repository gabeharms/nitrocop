- Investigate larger startup costs (process init/config load/result-cache read) with targeted instrumentation; tried micro-optimizations (session-hash clone/sort, JSON formatter serialization variants, lazy schema init, thread-pool refactors, and parking_lot RwLock swap) — all regressed or were noise.
- Explore reducing per-file stat-hit materialization overhead (cached diagnostic -> Diagnostic conversion and path cloning) without changing output semantics. Tried path-key changes (raw path string, u64 key map), entry-path caching, and manual loop materialization; only path-hash algorithm swap to FNV-1a helped.
- JSON formatter cloning was a major win when replaced by borrowed serialization; further field-level micro-tweaks (e.g., severity char) regressed. Prioritize non-formatter startup costs next.

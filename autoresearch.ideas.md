- Investigate remaining startup costs (process init / non-cache config work) with targeted instrumentation now that result/discovery/config caches use bincode+JSON fallback. Focus on post-cache hotspots rather than additional serializer micro-tuning.
- Re-baseline periodically with long sample sets before keeping micro-optimizations; recent 72-sample no-change run drifted to ~24.48ms mean (p50 24.46), indicating environment/noise shifts.
- Recent regressions/noise to avoid repeating: lockfile stamp fast-path, absolute-path canonicalization shortcut for config cache keys, conditional skip-summary clone, formatter write-call micro-tweaks (including compact `to_writer` streaming and compact `to_string` output), gem-path logging/instrumentation tweaks, gem-path cache key shape tweaks, embedded JSON from_slice/include_bytes conversion tweaks, autocorrect allowlist lazy-load/empty-path tweaks, preview-mode tier-map lazy/default shortcuts, discovery-cache hash-format churn, single-directory discovery sort/dedup bypass, result-cache session-hash micro-tuning, unix mtime conversion micro-optimizations in cache hot paths, fail-fast branch splitting in the linter hot loop, base_dir/config_dir exclude-check dedup logic, schema init early-return micro-optimization, and migrated-schema guard micro-optimization.
- For persistent on-disk cache keys, avoid `DefaultHasher` (randomized per process) and avoid unnecessary hash-format churn; both caused severe warm regressions via cache reuse disruption.
- Explore reducing per-file stat-hit materialization overhead (cached diagnostic -> Diagnostic conversion and path cloning) without changing output semantics. Tried path-key changes (raw path string, u64 key map), entry-path caching, manual loop materialization, conditional global sort check, reducing watched-dir stamps to immediate parents, and conditional skip-summary clone logic; only path-hash algorithm swap to FNV-1a helped.
- JSON formatter cloning was a major win when replaced by borrowed serialization; custom streaming serializer (serialize_seq over diagnostics) regressed. Keep current borrowed-Vec formatter and prioritize non-formatter startup costs next.
